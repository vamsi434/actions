name: github workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: checkout repo
          uses: actions/checkout@v4
        - name: Welcome Message
          run: echo "My first file"
        - name: List the file
          run: echo "ls -la"
        - name: display readme file
          run: echo "Hi Vamsi"

